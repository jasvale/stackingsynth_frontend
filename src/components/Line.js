const Line = () => {

    const hrStyle = {
        display: "block",
        height: "1px",
        border: "0",
        borderTop: "1px solid #ccc",
        margin: "1em 0",
        padding: "0",
    };

    return (
        <hr style={hrStyle} />
    );
};

export default Line;
